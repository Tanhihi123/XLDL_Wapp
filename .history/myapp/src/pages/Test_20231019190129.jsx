import React, { useState } from 'react';

const UploadImage = () => {

  const handleFileChange = (event) => {
    return (
      <div>
        tw-in
      </div>
    );
};

export default UploadImage;
