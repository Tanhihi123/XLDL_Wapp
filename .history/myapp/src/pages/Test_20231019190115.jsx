import React, { useState } from 'react';

const UploadImage = () => {

  const handleFileChange = (event) => {
    return (
      <div>
        inpt
      </div>
    );
};

export default UploadImage;
